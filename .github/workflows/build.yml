name: Build iOS Dylib

on:
  push:
    branches: [ "*" ]  # التجميع من أي فرع يتم الرفع عليه تلقائياً
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-14

    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Setup Official Theos
      run: |
        git clone --recursive https://github.com/theos/theos.git $HOME/theos
        echo "THEOS=$HOME/theos" >> $GITHUB_ENV

    - name: Download iOS SDK
      run: |
        git clone https://github.com/theos/sdks.git $HOME/theos/sdks || true

    - name: Force Compile
      run: |
        export THEOS=$HOME/theos
        # الأمر هذا يضمن التجميع حتى لو الملفات بفرع ثاني
        make package FINALPACKAGE=1

    - name: Upload Finished Dylib
      uses: actions/upload-artifact@v4
      with:
        name: ColorRoadModMenu-Dylib
        path: |
          .theos/obj/*.dylib
