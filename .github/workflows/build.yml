name: Build iOS Dylib

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-14

    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Setup Official Theos
      run: |
        git clone --recursive https://github.com/theos/theos.git $HOME/theos
        echo "THEOS=$HOME/theos" >> $GITHUB_ENV

    - name: Download iOS SDK
      run: |
        git clone https://github.com/theos/sdks.git $HOME/theos/sdks || true

    - name: Force File Sync
      run: |
        # عمل نسخة من الملفين بالحروف الصغيرة والكبيرة لضمان التطابق التام غصب عن النظام
        cp tweak.x Tweak.x 2>/dev/null || true
        cp Tweak.x tweak.x 2>/dev/null || true
        cp menu.mm Menu.mm 2>/dev/null || true
        cp Menu.mm menu.mm 2>/dev/null || true

    - name: Compile Dylib File
      run: |
        export THEOS=$HOME/theos
        make clean
        make package FINALPACKAGE=1
